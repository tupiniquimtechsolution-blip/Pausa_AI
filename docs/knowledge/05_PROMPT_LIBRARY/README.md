# 05_PROMPT_LIBRARY

Biblioteca versionada de prompts úteis ao projeto.

Para cada prompt, registrar ID, nome, objetivo, projeto, versão, status, texto, alterações posteriores e relação `superseded_by` quando houver uma versão mais nova. Prompts antigos relevantes não devem ser apagados silenciosamente.
