# 07_ARCHIVE

Material histórico, substituído ou abandonado que ainda precisa permanecer rastreável.

Arquivar não significa apagar. Registrar motivo, data/posição aproximada, substituto quando existir e um dos estados `historical`, `superseded` ou `abandoned`.
